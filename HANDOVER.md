# Project handover — start here

Last updated: 2026-09-22. Current phase: the requested stage-aware risk ranking, disease-weather family, four exposure families, researched establishment window and dashboard updates are implemented and verified locally and in the private API. Primary profile `stage_risks_v2`; methods `open-field-production-v2` / `location-evidence-v4`. **Global daily weather and land-hourly acquisition are complete; scientific validation, soil acquisition and the broader core handoff are not. The user subsequently authorized GitHub publication; hosted-Sites redeployment remains outside this request.**

## Implemented and verified

- **Current science/dashboard revision (2026-09-22):** `production.py` now ranks only defined events with ≥50% recurrence among ≥12 assessable winters, using each winter's own crop stages. Complete results stay in `by_id` and `demoted` with reasons; ties share ranks; empty rankings are valid. Disease-favourable weather is one family spanning complete flowering, fruit-development and harvest windows. New exposures: flowering cold-or-wet pollination proxy plus separate cold/dry supervisor hypothesis; fixed-hemisphere warm midwinter >21 °C hours; fruit Tmin ≤0 °C frost exposure; flowering/fruit rain<1 mm dry runs. Missing-hourly locations get separately labelled daily Tmax warm **days**, not estimated hours. Evergreen/unknown primary calendars do not support crop/chill-shortfall headlines; hypothetical exposures remain labelled.
- **Planting research implemented:** `planting.py` returns one independent regional establishment window. Florida: UF/IFAS mid-December–mid-February. Georgia: UGA winter transplanting, displayed approximately December–February. Santa Catarina: Embrapa-authored winter-while-dormant guidance, displayed approximately June–August. The last two month ranges are display conventions, not exact source-specified bounds. Unknown-region pins decline. The reviewed sources did not support the R's budbreak −105…−30 day planting rule; it was not adopted. Sources, formulas, assumptions and stock/irrigation limits: `docs/weather/production/README.md`.
- **Verification of this revision:** full server suite **96 passed**; **39 production/API tests passed again** after fixing a missing production-method field exposed by the frontend's stale-result guard. All 270 preset site-years preserved prior chill, dates and existing stage exposures; all 18 annual summaries and weather-content hashes were unchanged. Independent real-input reconstruction matched new metrics in 29 complete River Valley/Papanduva crop-years; a real daily-only land coordinate had 15 complete warm-day windows. Browser: 18 presets, 135 selected winter/stage views and 855 metric values; empty/evergreen states, keyboard/state retention, real shared-cell/daily-only requests, actual HTML/JSON exports; desktop/mobile inspected and 320/390/768/1440 px without page overflow. Both JS files and preview script passed syntax checks.
- **Delivered surfaces:** 18 `dist/snapshots/*.json` payloads, coordinate dashboard, and three-site `docs/weather/production/` HTML/JSON regenerated. Only changed API/science files and their tests were copied to server `code/`; only `blueberry-ui-api` was restarted. Local preview is `http://127.0.0.1:5173/`, with the existing private SSH forward; temporary network disconnects required reconnecting it during verification. Weather workers/data, soil attempts and unrelated server sessions were untouched. No hosted deployment or GitHub update.
- **Unresolved interpretation:** River Valley still models harvest **23 April–2 June**. Harvest heavy rain ranks at **14/14**, disease-weather at **12/14** assessable winters. The supervisor's early-April expectation remains a phenology discrepancy, not grounds to suppress the weather result. Pollination, warm-weather and dry-spell counts have no calibrated adverse-season cutoff. Existing chill/GDD/phenology constants, grid bias and the 19-cell hourly adapter limit remain unchanged. New-primary whole-production summaries now cover budbreak through harvest end; legacy profiles retain season-start totals.

- **Open-field production core implemented in Python** (`pipelines/weather/production.py`, 2026-09-12), replacing the planned R runner. Per-winter chain: chill (named definitions `below_7_2` and `bounded_0_7_2`) → GDD forcing → fixed offsets → stage exposures; both of Paul's classifiers reported from multi-year means, per-year counts and a two-thirds majority vote; risks ranked by frequency of winters with an event plus Wilson intervals; versioned constant profiles (`legacy_paul_v1` primary, 100 h and bounded variants for sensitivity). Fixes carried from the R review: same anchor for offsets and dates (no −31-day shift), fixed row schema for every year, complete-window refusal, suspect-rain decline, `anchor_required` for zero chill, no reference-panel scaling or weighted total. Packet for Waldo, Citra and Papanduva in `docs/weather/production/` (HTML, JSON, README) and server `reports/production/`. Server suite **73 tests pass** (18 new). All 90 site-year records at 50 h and 100 h match `stage_scenarios.json` exactly; Papanduva mean chill 199.4 h equals the reconciliation value. Results: Waldo/Citra Deciduous majority (11/15, 10/15 winters), median flowering 20 Jan / 26 Jan, harvest 31 Mar–10 May / 6 Apr–16 May, flowering-freeze frequency 0.27 / 0.20; Papanduva Semi-evergreen 10/15, flowering 6 Aug, harvest 15 Oct–24 Nov, two winters fail 100 h. Under the bounded 0–7.2 °C definition Citra becomes Transitional (chill-only majority) while its mean-based class stays Deciduous. Assumption-based exposures on uncorrected grid data; not validated phenology, not cultivar advice.
- **Production analysis in the UI** (2026-09-12): `location_api.py` (`location-evidence-v2`) attaches the `production` block for pilot coordinates with hourly data and an explicit `unavailable` reason otherwise; `dist/` section `00` renders system hypothesis, calendar, ranked risks, per-winter record, sensitivity and change/limitation lists above the annual cards. Snapshots regenerated for the three presets. Verified in headless Chrome: Papanduva/Waldo presets, Georgia through the live API (production available), London through the live API (production unavailable), tunnel-setup note, 390 px layout without overflow; `node --check` passed; server suite 75 tests. Start instructions in `docs/UI_RUNBOOK.md`. The hosted Sites snapshot was not redeployed.
- **Growing-cycle visualization** (2026-09-15): `dist/cycle.js` adds a monochrome month ruler for winter chill, bud development, flowering, fruit development, harvest and whole-cycle context. Stage selection shows existing exposure values and denominators; typical-cycle medians and timing spread are separate from individual-winter dates. Detailed tables remain expandable. Missing hourly data, failed chill and unavailable metrics are explicit; the new ruler declines evergreen-majority calendars without a management anchor. No scientific calculation, snapshot or API change. Browser smoke checks covered 135 stage windows and exposures across all 45 saved site-years, 18 typical stage views, keyboard/mouse controls, setup notes, four synthetic missing/unsupported states, and 320/390/768 px widths without page overflow. Desktop and mobile visually inspected. Real Chromium HTML/JSON downloads checked: production JSON unchanged, selected winter/stage retained; fixed cloned-select labels in HTML exports. `node --check` passed for both frontend scripts. No backend tests rerun or hosted deployment.
- **Southeastern evaluation panel and cell-keyed hourly lookup** (2026-09-15, user-authorized): `pipelines/weather/evaluation_sites.py` holds a 15-site registry (4 Georgia: Georgia/Alma pilot, Homerville, Valdosta, Folkston; 11 central/south Florida: Dole, Clear Springs, Astin East, Barben, River Valley, PF Berry pilots plus Wauchula, Sebring, Lake Placid, Okeechobee, Arcadia town pins) and writes `config/evaluation_sites.json` and `config/hourly_index.json` (MERRA-2 cell → stored hourly series). `evaluation_sites.py fetch` acquired hourly temperature/dewpoint for the five cells without a series (Homerville, Valdosta, Folkston, Okeechobee, Arcadia; 140,256 rows each, pilot QC, `data/normalized/pilot/evaluation/`), **0 bytes transferred** because their 5×5-cell zarr chunks were already cached; ten sites reused existing cells (Wauchula→River Valley, Sebring/Lake Placid→Barben, Clear Springs→Astin North East cell). `location_api.py` (`location-evidence-v3`) now resolves hourly by source cell for any pin and reports `hourly_source` (cell centre, sharing site, distance, resolution note); the UI shows it in the availability strip. Snapshots regenerated for 18 sites (three references + panel), split into `dist/snapshots.json` index + `dist/snapshots/*.json` by `scripts/split_snapshots.py`; presets are grouped Reference / Georgia / Central Florida / South Florida. Server suite **79 tests pass** (4 new). Browser-checked: Arcadia (Evergreen majority → ruler declines as designed), Wauchula and Homerville presets, a live non-panel pin inside Barben's cell (packet available, "shared with Barben"), a live Tallahassee-area pin (daily only), no page errors, 390 px without overflow. Town pins are city centres, not orchard centroids; every pin inside one 0.5°×0.625° cell receives the same chill and calendar. No global hourly, no soil restart, no hosted redeployment.
- **Risk-factor research review** (2026-09-15): `docs/weather/RISK_FACTOR_RESEARCH.md` synthesises eight parallel literature scouts (chill, forcing, freeze, heat, disease, water, pollination/pests, classification) into a formula scorecard and a stage-by-stage catalogue of ~35 computable metrics with sources and confidence. Data check `pipelines/weather/chill_comparison.py` → `docs/weather/chill_comparison.json`: on our 18 hourly cells the current chill rule (T < 7.2 °C, Nov–May, no lower bound) over-counts the standard 0–7.2 °C Oct–Feb band by 60–200 h in Georgia; the band matches FAWN Sebring (104 vs 108 h) but undercounts Gainesville (346/296 vs 503 h, grid warm bias); Papanduva has 183 chill hours but 26 Dynamic-Model chill portions, equal to Waldo (28.5), so its chill-hour class is a model artefact. Main change recommendations: Dynamic Model as primary chill metric; cultivar-class chill requirements instead of 50 h; GDD-from-1-Jan bloom model instead of chill date + 150 GDD; species-specific harvest offsets; stage-aware freeze thresholds with duration and protection feasibility; leaf-wetness-based per-disease rules; pollination-favourable days; stage water balance; management-anchored evergreen calendar. Research only; `production.py` unchanged.
- **Supervisor review and future plan** (2026-09-21): `docs/weather/SUPERVISOR_REVIEW_RIVER_VALLEY.md` records Paul's decisions (window-overlap and frequency gate for risks, disease ranked, four new metrics, single planting window from his R code, deprioritised items, management layer after field risks). `docs/research/2026-09-21/` holds eight scout reports; `docs/FUTURE_PLAN.md` consolidates them into six phases with builder-day estimates, milestones, human decisions and data requests. Paul's new R file (`southern_brazil_blueberry_climate_analogs.R`) is a private local input, git-ignored; its planting rule (budbreak −105…−30 d), chill-portion code and constant differences are documented in report 01. Plain-language HTML of the risk-factor review added. No code or formula change.
- **Land hourly cache complete** (started 2026-09-21 20:33 UTC, finished 2026-09-22 01:12 UTC; user-authorized, lifting the earlier "no global hourly" exclusion): `pipelines/weather/global_hourly.py` filled the raw hourly store (T2M, T2MDEW, 2010–2025) for all 3,479 land-containing 5×5 blocks (of 8,468): 6,958 / 6,958 jobs validated, 20,885 cached objects, **40.5 GB on disk** (the 28.7 GB pre-run estimate used the mean size of the earlier pilot chunks, which ran small; the conservative decoded bound of 146 GB and the 200 GiB / 500 GiB gates held). Each block was decoded, range-checked with `quality()` and recorded in the shared `tiles` table. State `state/global_hourly.json` reads `complete`; note "grid data, not station-validated". Two tests (land-only block selection, time-extent refusal) pass on the server. Do not rerun. Extraction code still reads only the 19 indexed cells; wiring any land cell to the cache is Phase 3.1 of the plan.
- Full R review: `docs/weather/R_FULL_CODE_REVIEW.md` records a sequential read of all 5,759 lines, all 51 top-level functions and the full output chain. **22 local source-pinned characterization checks pass** via `scripts/audit_paul_reference.R` (17 reviewed helpers plus syntax/scalar diagnostics); wrong-source fingerprint refusal also verified. New findings include single-row reference scoring, session-dependent analogue labels, incomplete-year schema splitting, undefined optional genotype variable and positional master-answer lookup, alongside the −31-day northern anchor and missingness defects. These are reproduced source behaviors, not fixes or scientific validation. Original R/transcript remain private and unchanged; no monolithic run, download or deployment.
- `docs/weather/R_ADAPTATION_PLAN.md` records the R/data contract and supervisor gates. The Python production core and dashboard integration now exist; scientific calibration and broader evaluation remain pending.

- NASA POWER global daily archive: 2010–2025, with 2011–2025 baseline; all 7,257 planned tiles completed. Meteorology and solar remain on their native grids.
- Fourteen pilot locations plus five evaluation cells: daily weather from the global archive, hourly temperature/dewpoint per source cell; 42 pilot Parquet files with no missing values, 5 evaluation hourly files with pilot QC.
- Resumable downloader, checksums, bounded retries/resources, atomic publication, worker locking and coordinate extraction with land/offshore handling.
- Ten global humidity gaps located. Eighty-nine rainfall cell-days above the 1,000 mm/day investigation threshold identified; original values preserved. Extractions expose a suspect-rain flag and admissibility warning.
- Station screening plus a 2020 FAWN Citra / INMET Major Vieira and Rio Negrinho pilot. Warm minimum-temperature biases and missed below-zero days are documented; no correction fitted.
- Most recent full backend suite: **96 tests passed**, 2026-09-22; the subsequent analysis-method metadata fix passed 39 production/API tests and browser verification.
- UI implemented in `dist/`: 18 derived presets, regional establishment guidance, stage risk/exposure details, coordinate/system inputs, annual weather cards/charts, soil profiles, qualitative management notes, provenance and HTML/JSON exports. No cultivar rankings or calibrated tunnel effects.
- Connected prototype implemented in `pipelines/weather/location_api.py` plus `scripts/preview-ui.mjs`: loopback-only API and fixed SSH-forwarded local proxy; a new London coordinate returned 5,479 daily rows with unavailable chill/soil explicitly shown. Static hosting contains saved snapshots only, not private-network access. Startup, scope and remaining production work: `docs/UI_RUNBOOK.md`.
- UI checks include desktop/mobile inspection, presets and setup controls. Chromium download-file verification completed on 2026-09-15 for the cycle view; broader cross-browser/report compatibility remains unverified. These checks establish software behavior, not scientific validity.
- Found and excluded Natural Earth's explicitly labelled artificial “Null island” polygon; `land-mask-v2` records the exclusion in provenance. Regression includes null-labelled real land; remote snapshots regenerated from unchanged archives.
- Soil acquisition started: 157/630 records downloaded; Citra/Papanduva/Waldo complete at 45 each, Astin East 22. Five properties, three depths, mean/Q0.05/Q0.95. WCS timed out three times at Astin East sand 5–15 cm Q0.05; worker stopped `blocked_source`, no active soil worker. Do not reset attempts blindly. Raw/normalized soil stays on server. Returned WCS pixels are geographic/reprojected, not asserted native SoilGrids cells; scientific/native-grid validation pending.
- Consolidated inventory implemented: `pipelines/weather/data_inventory.py`, server `reports/data_inventory.json`; readable map in `docs/weather/DATA_CATALOG.md`. Soil runbook: `docs/weather/SOIL_DATA_RUNBOOK.md`. No weather redownload or bulk-data duplication.
- Exploratory 50/100-hour stage comparison implemented in `pipelines/weather/stage_scenarios.py`; offline HTML/JSON in `docs/weather/climate_evidence/stage_scenarios.*`. Paul-derived UTC assumptions, not calibrated cultivar predictions. Citra/Waldo have 15 paired winters; Papanduva has 13 because 2015/2023 do not reach 100 hours. Complete-window and suspect-rain refusal enforced; no downloads.
- Paul's method audit and controlled UTC chill sensitivity completed: `docs/weather/PAUL_RECONCILIATION.md`, companion JSON and `pipelines/weather/reconcile_methods.py`. Papanduva mean changes from 178.9 to 199.4 hours when matching the supplied R window/threshold on our data; this is not an exact reproduction of his 209.4-hour report. Time standard, historical period and generating configuration remain unresolved. No R workflow execution or cultivar modelling.
- Supervisor demonstration implemented: `docs/weather/climate_evidence/climate_evidence.html` and companion JSON. Papanduva/Citra/Waldo monthly climate, annual variability, reference-window chill, dry spells, threshold exposure, hemisphere-aligned seasons and limitations. Offline generation from existing server archives; no new weather download. Opening layout/charts visually inspected.

## Next actions, in order

**Next work requires the relevant authorization/decision:** follow remaining scope in `docs/FUTURE_PLAN.md`, not its superseded planting-offset proposal. Shared metric metadata, recurrence gating, disease-family ranking, four exposure families, regional establishment guidance and updated snapshots are complete. Remaining work includes typed-result validation, production chill portions and cultivar-class phenology, the any-land-cell adapter over the completed hourly cache, durable jobs/cache and authenticated team access. Field/phenology validation and station-bias work require pre-registered expectations and suitable observations. Management/genotype layers remain deferred. Review the provisional reporting gate and regional stock assumptions with Paul/Gerardo. Do not port the composite suitability/analogue score or start new acquisition/deployment from this handover alone.

UI/serving work: production outputs and the growing-cycle ruler are implemented. Authenticated hosted archive connectivity, durable jobs/cache, granular missing-data states and broader export compatibility remain needed. "Live" is historical analysis, not a forecast. The initial owner-private snapshot UI was published through Sites from commit `b9cb0ff`; registration is in `.openai/hosting.json`. It has not been redeployed with the newer production UI.

2026-09-11 management refinement: `docs/weather/LOCATION_MANAGEMENT_PLAN.md` specifies subsequent soil/cover/root-zone layers. Preserve completed soil records and source-blocked attempts; do not let the soil outage block the new open-ground calendar/risk milestone. ET0 and tunnel models are not implemented. Cultivar work remains deferred. Continue the weather-core gates below alongside this work.

Supervisor direction relayed by the user on 2026-09-08: defer cultivar/breeder data to Part 3. The three-site climate evidence report and exploratory seasonal comparison are implemented. The user subsequently requested this initial UI. Finish the weather core gates alongside supervisor review; do not block this work on cultivar data or imply the full app is complete.

1. Finish corrupted compressed-source response tests.
2. Extend the implemented three-site indicator demonstration to diverse global probes. No invented phenology or cultivar rankings. The supervisor's initial HTML evidence report is ready; broader scientific validation remains pending.
3. Ensure derived calculations honor suspect/missing rainfall and incomplete windows.
4. Audit versions, resource accounting, provenance and completion criteria; write final weather handoff and pause the existing monitor only when those criteria pass.

Multi-year station validation and rainfall measurement-convention checks remain research work. Copernicus comparison requires user credentials and accepted terms; do not expose credentials. Soil is optional. A research UI exists; no cultivar recommendation model has been built.

## Where things live

- Code: `pipelines/weather/`; tests: `tests/weather/`.
- Production analysis (system, calendar, stage risks): `pipelines/weather/production.py`; packet and reproduction notes in `docs/weather/production/`; server output `reports/production/`.
- Server project: `/media/fpt/fpt2/Weather_Claude`; deployed scripts in `code/`; Python in `env/bin/python`.
- Weather arrays: server `data/normalized/global/`; pilot, evaluation and station files under `data/normalized/`; site registries in server `config/sites.json`, `config/evaluation_sites.json`, `config/hourly_index.json`.
- Operational access and commands: `docs/weather/SERVER_RUNBOOK.md`. Credentials are not included.
- Full scope and completion gates: `DATA_ACQUISITION_HANDOFF.md`.
- Research architecture: `IMPLEMENTATION_PLAN.md`.
- Findings: `docs/weather/VALIDATION_FINDINGS.md` and `REGIONAL_STATION_PILOT.md`.
- Progress history: `docs/PROGRESS_LOG.md`.

## Continuity and publication

Follow `AGENTS.md` for evidence and handover updates. On 2026-09-22 the user lifted the earlier no-GitHub restriction and requested publication with relevant comments. Publish the reviewed implementation, derived reports, tests and documentation to the existing `origin/main`, with a commit message recording scope, verification and scientific limits. Remote history was checked before preparation and matched local HEAD; this handover does not claim its own push succeeded. Verify the remote commit before reporting publication. No independently installed post-prompt publication hook exists. Original R workflows, supervisor source materials, credentials and bulk data remain outside publication.

The source reports, supervisor materials and original R script remain local reference inputs and are not part of this publication. Existing absolute local links in older documents may not work on GitHub; use repository-relative paths listed here.

Latest action (2026-09-22): prepared the verified science/dashboard revision for user-authorized GitHub publication, with method/source notes, test evidence and unresolved phenology limits. The local UI loaded and its private API health endpoint returned ready when the user requested launch. Publication review found no credentials in the selected changes; removed a machine-specific checkout path from the UI runbook. Tests were not rerun for this documentation/publication-only step; the implementation evidence above remains the recorded verification. No new weather acquisition, model calibration, cultivar work or hosted redeployment.
